/*====================== main.c =================*/
#include <stdio.h>
#include <stdlib.h>
#include "tipos_compostos.h"
/* Aqui main ;( */
int main(void){
	char *msg;
	circulo *c = (circulo*) malloc(sizeof(circulo));

	c.msg = NULL;

	
	return (0);
}